/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package smart_corkscrew_homework;

/**
 *Smart Corkscrew Homework. 
 * Methodology of Programming 2
 * @author Beltran Castro Carlos Ricardo
 * Neptun code: VY6V8S
 */
public class ScrewCounter {
     // This is where the counter function works 
    private final String name;     // counter name
        private final int maxCount;    // maximum value
        private int count;             // current value
    private Object StdOut;

        // create a new counter with the given parameters
        public ScrewCounter(String id, int max) {
            name = id;
            maxCount = max;
            count = 0;
        } 

        // increment the counter by 1
        public void increment() {
            if (count < maxCount) count++;
        } 

        // return the current count
        public int value() {
            return count;
        } 

        // return a string representation of this counter
        public ScrewCounter() {
        } 
            // print results
        for (int i = 0; i < n; i++) {
            StdOut.println(hits[i]);
        }

    private static class i<T0, T1> {

        public i() {
        }
    }

    private static class n {

        public n() {
        }
    }
    }
       
}
